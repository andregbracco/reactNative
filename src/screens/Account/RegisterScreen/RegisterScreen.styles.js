import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    image: {
        width: '60%',
        height: 230,
        marginTop: 20,
        marginHorizontal: 80
    },
    content: {
        marginHorizontal: 40,
    }
});
